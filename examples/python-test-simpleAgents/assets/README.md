# Assets

Place multimodal example files here (for example `test-invoice.jpeg` for invoice image scripts).

Image files are typically **not** committed; the runners error with a clear message if a file is missing.
