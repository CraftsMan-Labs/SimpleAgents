id: invoice-image-terminal-node-golden
workflow_path: ../../workflows/email-classification/test.yaml
dataset_path: generated/invoice-image-terminal-eval.dataset.jsonl
max_concurrency: 1

execution:
  node_llm_streaming: false
workflow_options:
  telemetry:
    enabled: false

comparison:
  mode: paths
  paths:
    - $.terminal_node
