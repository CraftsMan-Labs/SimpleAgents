# Assets

Multimodal example files used only by Node/Bun demos can live here.

Invoice image demos read the same JPEG path as Python:  
`examples/python-test-simpleAgents/assets/test-invoice.jpeg` (via `pathToPythonExamplesAsset(...)`).
