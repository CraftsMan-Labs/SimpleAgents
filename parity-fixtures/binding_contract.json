{
  "shared_cases": {
    "request": {
      "required_message_roles": ["system", "user", "assistant", "tool"],
      "completion_modes": ["standard", "healed_json", "schema"]
    },
    "response": {
      "required_fields": ["content", "finish_reason", "usage.total_tokens"]
    },
    "healing": {
      "required_fields": ["value", "confidence"],
      "modes": ["healed_json", "schema"]
    },
    "streaming": {
      "event_types": ["delta", "error", "done"],
      "finish_reasons": ["stop", "length", "content_filter", "tool_calls"]
    },
    "tool_call": {
      "required_fields": ["id", "tool_type", "function.name", "function.arguments"]
    }
  },
  "ffi": {
    "required_c_symbols": [
      "sa_client_new(",
      "sa_client_free(",
      "sa_complete(",
      "sa_stream(",
      "sa_run_workflow(",
      "sa_stream_workflow(",
      "sa_resume(",
      "sa_last_error_message(void)",
      "typedef int32_t (*SAStreamCallback)(const char *event_json, void *user_data);"
    ]
  },
  "go": {
    "required_api_symbols": [
      "type Client struct",
      "func NewClient(",
      "func (c *Client) Complete(",
      "func (c *Client) StreamComplete(",
      "func (c *Client) Run(",
      "func (c *Client) Stream(",
      "func (c *Client) Resume("
    ]
  },
  "node": {
    "required_type_symbols": [
      "struct CompletionResult",
      "struct StreamEvent",
      "run(",
      "stream(",
      "resume("
    ],
    "required_runtime_exports": [
      "Client"
    ]
  },
  "python": {
    "required_type_symbols": [
      "class ResponseWithMetadata",
      "class StreamChunk",
      "class PyStructuredEvent",
      "class Message",
      "class Role",
      "class ContentPart",
      "stream: Literal[True]"
    ],
    "required_api_symbols": [
      "class Client:",
      "def complete(",
      "def run(",
      "def stream(",
      "def resume("
    ]
  }
}
